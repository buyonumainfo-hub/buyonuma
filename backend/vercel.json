{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",  // ← Change to actual path
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.js"  // ← Match the path
    }
  ]
}
